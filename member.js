function skillsMember() {
    // Get the member's skills
    var memberSkills = getMemberSkills();
    // Get the member's level
    var memberLevel = getMemberLevel();
    // Get the member's name
    var memberName = getMemberName();
    // Get the member's age
    var memberAge = getMemberAge();
    // Get the member's location
    var memberLocation = getMemberLocation();
}